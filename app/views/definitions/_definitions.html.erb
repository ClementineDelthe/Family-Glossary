<ul>
  <% @definitions.each do |definition|%>
  <hr>
  <li ><h4><%= link_to(definition.title, definition_path(definition)) %></h4> <i class="fas fa-chevron-right" style="color: #130872;"></i> <%= definition.content %>
  </li>
  <hr>
  <%end%>
</ul>
